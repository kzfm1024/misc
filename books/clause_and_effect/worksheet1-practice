?- pair(percical, X).
false.

?- pair(percival, X).
X = lucinda ;
X = camilla.

?- pair(apollo, daphne).
false.

?- pair(camilla, X).
false.

?- pair(X, lucinda).
X = bertram ;
X = percival.

?- pair(X, X).
false.

?- pair(bertram, lucinda).
true.

?- pair(X, fido).
false.

?- pair(X, Y).
X = bertram,
Y = lucinda ;
X = bertram,
Y = camilla ;
X = percival,
Y = lucinda ;
X = percival,
Y = camilla.

?- 